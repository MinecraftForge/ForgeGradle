allprojects { project ->
    if (project == project.rootProject)
        return;

    afterEvaluate {
        project.tasks.jar {
            destinationDir = project.file("@RECOMP_DIR@/"+project.name);
            archiveName = "recompiled.jar";
        }
    }
}